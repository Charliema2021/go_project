底层逻辑就是通过小写首字母，使得包里的实例无法被其他包直接调用
然后用工厂模式提供一个方法，去get这个值~~~
目前有两种方法

一个是Set

func （ 结构体名称)SetXxx（参数列表）返回值列表{

    // 加入数据验证的业务逻辑
    var 字段名=参数
}


一个是Get

func  （结构体名称)GetXxx{
    return var.age // 小写字母的哪个实例~~~
}
